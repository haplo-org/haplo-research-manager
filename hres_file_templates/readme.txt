title: File Templates
--

File templates allow institutions to manage letterheads and other templates. Templates can be added for a particular institution.

h3(handler). do/hres-file-templates/admin

Where to manage templates.

h3(action). "hres_file_templates:configure"

By default, users in the @Administrators@ group are allowed to configure templates. Extend this action if required.