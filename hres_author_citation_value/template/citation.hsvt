if(object) {
    if(value.cite) {
        ifRenderingForWebPublisher() {
            std:web-publisher:object:link(object value.cite)
        } else {
            <a href=std:object:url(object)> value.cite </a>
        }
    } else {
        std:object:link(object)
    }
} else {
    value.cite
}
