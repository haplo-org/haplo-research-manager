
each(sections) {
  if(display) {
    std:web-publisher:template("haplo:publication-common:ui:panel") {
      <div class="card border-0 bg-light mb-1">
        <div class="card-body">
          <h2> section.title </h2>
          render(display)
        </div>
      </div>
    }
  }
}
