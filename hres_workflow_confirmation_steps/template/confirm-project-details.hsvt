
if(useTransitionSteps) { std:workflow:transition-steps:navigation(M "hres:confirm_details") }

within(confirmDetails) {
    template:std:ui:confirm()
}

<p> "&nbsp;" </p>

std:object(displayObject)
if(deferredDates) { render(deferredDates) }
